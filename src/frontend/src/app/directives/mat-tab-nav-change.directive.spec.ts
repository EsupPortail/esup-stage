import { MatTabNavChangeDirective } from './mat-tab-nav-change.directive';

describe('MatTabAfterDirective', () => {
  it('should create an instance', () => {
    const directive = new MatTabNavChangeDirective();
    expect(directive).toBeTruthy();
  });
});
