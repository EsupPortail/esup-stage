import { RoleLibellePipe } from './role-libelle.pipe';

describe('RoleLibellePipe', () => {
  it('create an instance', () => {
    const pipe = new RoleLibellePipe();
    expect(pipe).toBeTruthy();
  });
});
