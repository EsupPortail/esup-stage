kind: pipeline
name: default

steps:
- name: test
  image: maven:3-openjdk-11-slim
  commands:
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
  - mvn test -B
trigger:
  branch:
  - dev
